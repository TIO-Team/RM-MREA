# RM-MREA

Codes for "Relation model-assisted multi-region evolutionary algorithm for expensive constrained optimization".

RM-MREA is implemented in MATLAB. The code can be run independently. To use it, simply execute the main script (e.g., `Main_Exp.m`) directly in your MATLAB environment.
